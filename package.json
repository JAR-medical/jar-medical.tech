{
  "name": "jar-medical-tech",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Static website for jar-medical.tech (GitHub Pages). No build step - this package.json exists only so the test suite can load ES modules.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
